<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/pop_card_layout"
    android:layout_width="match_parent"
    android:visibility="gone"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/pop_intpu_edit_card"
        android:layout_width="348dp"
        android:layout_height="50dp"
        android:layout_marginTop="11dp"
        android:background="@drawable/pop_intpu"
        android:focusable="false"
        android:gravity="center"
        android:enabled="false"
        android:hint="请刷卡"
        android:inputType="numberDecimal"
        android:textColor="@color/color_666"
        android:textColorHint="#ffcccccc"
        android:textCursorDrawable="@color/transparent"
        android:textSize="18sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/image_vip"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/pop_intpu_edit_card" />
</android.support.constraint.ConstraintLayout>