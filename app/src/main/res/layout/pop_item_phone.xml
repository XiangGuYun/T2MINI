<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:id="@+id/pop_phone_layout"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/pop_intpu_edit_phone"
        android:layout_width="348dp"
        android:layout_height="50dp"
        android:layout_marginTop="11dp"
        android:background="@drawable/pop_intpu"
        android:gravity="center"
        android:hint="请输入手机号码"
        android:inputType="numberDecimal"
        android:textColor="@color/color_666"
        android:textColorHint="#ffcccccc"
        android:textCursorDrawable="@color/transparent"
        android:textSize="18sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.yp.payment.view.VirtualKeyboardView
        android:id="@+id/virtualKeyboardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@+id/pop_intpu_edit_phone" />
</androidx.constraintlayout.widget.ConstraintLayout>